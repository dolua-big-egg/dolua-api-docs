# SPI0 固定脚：pin66 GPIO8=CS, pin67 MOSI, pin28 MISO, pin29 SCLK，和 UART2 默认脚重叠。
# 必须把 UART2 从默认脚挪走，否则和 SPI0 抢脚。
# pin_map=1 → UART2 改到 pad 21/22；日志若走 UART2 再配 print_route/log_route=2。

[lua]
print_route=1    # Lua print() 输出串口，1/2/3
log_route=1      # log 模块输出串口，1/2/3（与 print 独立）

[uart.2]
pin_map=1
