# 远程升级

HTTP 拉固件旁路包、Lua 脚本包或配置，再确认 / 回滚。脚本 OTA 回滚开关在 `rtu_config.cfg` 的 `[loader] script_rollback_enable`。

落盘对照 [http_file_ublob](../../network/http/http_file_ublob)、[http_file_lfs](../../network/http/http_file_lfs)；脚本接口见 [script](../../../../docs/NT26/api/module/script.md)。
