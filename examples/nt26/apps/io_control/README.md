# IO 采集与控制

GPIO 输入采样、输出控制、边沿上报、简单时序。不要和 `rtu_config.cfg` 里的 `net_led` / `wtg` / IO 上报抢同一脚。

接口对照 [gpio/normal](../../peripherals/gpio/normal)、[interrupt](../../peripherals/gpio/interrupt)、[io_task](../../peripherals/gpio/io_task)。
